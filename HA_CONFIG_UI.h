#pragma once
static const char HA_CONFIG_UI[] PROGMEM=R"ha(
<section class="card section"><h2>Home Assistant MQTT</h2><p>Uses the broker credentials on the <a href="/mqtt">existing MQTT settings page</a>. The existing message format can stay Disabled for Home Assistant only, or remain enabled for Domoticz alongside Home Assistant. Its formats and topics are unchanged.</p><form method="post" action="/home-assistant/save"><div class="field"><label><input name="enabled" type="checkbox" {enabled}> Enable Home Assistant discovery and telemetry</label></div><div class="field"><label for="prefix">Discovery prefix</label><input id="prefix" name="prefix" maxlength="48" value="{prefix}" required><span class="help">Match the Home Assistant MQTT discovery prefix. Default: homeassistant.</span></div><div class="actions"><button>Save and restart ECU</button></div></form></section>
<section class="card section"><h2>Solar energy</h2><p>After discovery, open Home Assistant's Energy settings, add Solar production, and select the fleet's <strong>Solar energy</strong> sensor. Select its Solar power sensor too if your Home Assistant version offers that option. Alternatively, choose the individual inverter energy sensors. Do not select both the fleet and its inverters: that would count production twice.</p><p>The solar counter measures energy observed by this ECU, in kWh. It is independent of the daily history counter and does not reset at midnight. Counters are recovered from retained MQTT checkpoints on the broker. No periodic ECU flash writes are added. Enable broker persistence and preserve these topics. Unexpected power loss can lose energy since the last acknowledged checkpoint; lost broker checkpoints cannot be reconstructed from ECU RAM.</p></section>
<section class="card section"><h2>Output control</h2><p>Each inverter has a <strong>Per-input power limit</strong> number, from 20 to 500 W per connected input. This is not a whole-inverter wattage or a percentage. 500 requests normal maximum output. Calibration is applied by the existing inverter control code.</p><p>Controls require recent inverter telemetry. A command is reported as applied only after the inverter confirms it. Commands from an older MQTT connection are rejected. Home Assistant limit commands update RAM only; they do not save a new boot-time limit to flash.</p></section>
)ha";
